import { DivergingLikertExample } from './example'
import { DivergingColorsExample } from './colors'
export { Root } from './Root'

# Diverging Likert Bars

See the file `docs-src/example.js` for a demonstration of usage.

<DivergingLikertExample />

## Custom Colors

Check the syntax in `docs-src/colors.js`.

<DivergingColorsExample />